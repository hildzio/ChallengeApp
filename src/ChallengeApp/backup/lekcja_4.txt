/*Pomysł na zadanie po lekcji 4 : Zebaranie cen artykułów codziennej potrzeby z różnych okresów w ciągu ostatniego roku,
 wyliczenie wzrostu/spadku ceny w ciągu roku, średniej rocznej ceny i posortowanie ich względem wartości.*/

using System;

namespace ChallengeApp
{
    class Program
    {
        static void Main(string[] args)
        {
            int a, b, result_add, h1;
            a = 10;
            b = 11;
            double c = 10.03;
            var d = 3.14;
            var e = 23;
            float f = 34.334f;
            decimal convert_add, c1, f1, g = 10;
            long h = 235991;
            var name = "Adam";

            h1 = (int)h;
            f1 = (decimal)f;
            c1 = (decimal)c;
            result_add = a + h1;
            var result_dev = f / d;
            var result_sub = e - a;
            var result_mply = b * f;

            convert_add = c1 + f1 - g;
            var mix_string = name + d;

            if (args.Length > 0)
            {
                Console.WriteLine("Są arumenty w tablicy i to pierwszy z nich : " + args[0]);
            }
            else
            {
                Console.WriteLine("Brak arumentów, stąd widzisz tylko ten komunikat");
                /* Console.WriteLine("Na pocieszenie masz wynik działania :");*/
            }
            /* Console.WriteLine($"Pierwszy tekst w .NET, zwiastujący zmiany :) {args[0]}");
             Console.WriteLine("Drugi tekst "+ args[0]);*/
        }
    }
}
